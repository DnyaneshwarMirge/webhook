pipeline {
    agent any

    stages {
        stage('webhook practice') {
            steps {
                git branch: 'main', url: 'https://github.com/DnyaneshwarMirge/webhook.git'
            }
        }
    }
}
#12345
